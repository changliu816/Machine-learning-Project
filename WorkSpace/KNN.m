function [ yHat ] = KNN( Xtrain, Ytrain, Xtest, threshold, k)
    [mtest, ntest] = size(Xtest);
    [mtrain, ntrain] = size(Xtrain);
    
    yHat = ones(mtest, 1);
    for i = 1:mtest
        
    end
end

